A simple window switching tmux fzf plug-in


# use

`c-h` switch last window arrow sessions

`c-l` search all window arrow sessions

# install

by tpm plugin

```
set -g @plugin 'edte/tmux-fzf'
```

# refs
https://github.com/sainnhe/tmux-fzf
